;
; soundtypes - configuration file
;

global.input				../../samples/voices/Vox.wav
;global.input				../../samples/mixtures/Beethoven_Symph7_short.wav
;global.input				../../samples/instruments/bass.wav
;global.input				../../samples/instruments/Beethoven.wav

global.outputpath			tests
global.verbose				0

analysis.win.type			hanning
analysis.win.size			4096
analysis.win.hop			256
analysis.maxfreq			5512.5
analysis.ampscale			log
analysis.normalize			1
analysis.descriptors		centroid spread skewness kurtosis energy
analysis.modeling			0
analysis.savedesc			0

types.algorithm				gmm
types.centroids				0.98
types.dimensions			0  
types.showlabels			0
types.rematch				tests/Beethoven.wav.win4096.hop256.log.gmm_95%.ngrams8.average_generate_order6.centroid.spread.skewness.kurtosis.energy.normalized.centroids.txt
types.rematch.amount		.45
types.rematch.distance		kullback
types.rematch.xmode			morph ; flatten ; mult ; morph
types.rematch.envelope		0 ; 90

probabilities.ngrams		8
probabilities.weight		1 ; 1:0  ; 1 
probabilities.merge			tests/Beethoven.wav.win4096.hop256.log.gmm_95%.ngrams8.average_generate_order6.centroid.spread.skewness.kurtosis.energy.normalized.probabilities1.txt
probabilities.merge.amount	0

decomposition.performance   rebuild ; generate
decomposition.order			1
decomposition.distance	    taxicab
decomposition.algorithm		average
decomposition.phasor		1
decomposition.stretch		1
decomposition.pitch		    1
decomposition.formants	    1
decomposition.envelope		80
decomposition.threshold		0
decomposition.rescaling		1
decomposition.jitter		0
decomposition.stereo.width	0
decomposition.stereo.delay	90

;analysis.fftpadding		1

;types.rematch.amount		1:.4

;decomposition.phasor		1:.3
;decomposition.stretch		1:1.5
;decomposition.pitch		1:.5
;decomposition.formants	    1:2
;decomposition.envelope		80:20
;decomposition.threshold	0:10
;decomposition.xsynth		file.wav:.3:1
;decomposition.filter		centroid > .4, spread = .5, skewness < .3


; * help *
;
; - max analysis frequency is nyquist

; - available features: centroid spread skewness kurtosis
; 	irregularity slope decrease energy hfc zcr flux f0 inharmonicity

; - available windows: hanning hamming blackman bartlett

; - available amplitude scales: lin log pow

; - available clustering algorithms: gmm, kmeans

; - the number of centroids is a ratio of the number of frames; 
;   max is 1, 0 means no clustering

; - if dimensions are 0 pca reduction is not applied, otherwise is the # of pcas

; - types of performances are: none, rebuild, generate

; - types.rematch.xmode = mult, flatten (needs coefficients), morph

; - available distance measures: euclid mahalanobis taxicab cosim kullback

; - if probabilities.merge is not "none" then the markov model is merged with;
;   the specified one, at the given amount

; - available algorithms for decomposition: average weighted random closest sequence

; - phasors are only used with the seuqnece algorithm

; - pitch, formants and stretch are ratios of original file

; - threshold is used for denoising

; - envelope is the number of coefficient for envelope preservation
;   (needed for formants; optional for pitch)

; - jitter is specified in samples

; - stereowidth (ratio) and stereodelay (samples) are used to generate a stereo
;   file; if stereowidth is 0 a mono file is generated

; * eof *